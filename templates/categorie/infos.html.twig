{% extends 'base.html.twig' %}

    


{% block title %}Categorie{% endblock %}

{% block body %}
    {# {% include 'japon.html.twig' %}  #}
    {{ include("categorie/japon.html.twig") }}

    {# <div class="col-md-6 mt-4">
      <div class="card-body bg-transparent">
        <h2 class="card-title fw-bolder fst-italic text-decoration-underline mx-auto">{{ categorie.categorie }}</h2>
      </div>
    </div> #}
   
        {# <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ categorie.intitule }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ categorie.categorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table> #}

 

    {# <a href="{{ path('app_categorie_index') }}">back to list</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a> #}
  
    {# {{ include('categorie/_delete_form.html.twig') }} #}
{% endblock %}
