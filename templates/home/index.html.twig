{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<header>
        <div class="ms-5" style="width: 1700px;">
                                <img src="images/gallery/arbre.jpg" class="img-fluid" alt="">
                        </div>
</header>
<main>
        <div class="container text-center">
                <div class="row mt-5 pb-5">
        {% if categorie is not null %}
        {% for hobby in categorie.hobbies %}

        <div class="col-md-6">
                <img width="450" src="{{ asset("images/" ~ hobby.photo ) }}" alt="{{ hobby.pays.nom }}">
        </div>
        <div class="col-md-6">
                <h1>{{ hobby.pays.nom}}</h1>
                <p>{{ hobby.pays.description}}</p>
        </div>

{% endfor %}
        {% else %}
        pas de resultat
{% endif %}
                </div>
        </div>
</main>

{# <table class="table">
        <thead>
            <tr>
                <th>photo</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>

{% for hobby in categorie.hobbies %}
<tr>
    <td><img width="200" src="{{ asset("images/" ~ hobby.photo ) }}" alt="{{ hobby.pays.nom }}"></td>
    <td>{{ categorie.categorie}}</td>
    
</tr>
{% else %}
<tr>
    <td colspan="2">no records found</td>
</tr>
{% endfor %}
</tbody>
</table> #}
{% endblock %}