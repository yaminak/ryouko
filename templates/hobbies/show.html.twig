{% extends 'base.html.twig' %}

{% block title %}Hobbies{% endblock %}

{% block body %}
    <h1>Hobbies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hobby.id }}</td>
            </tr>
            <tr>
                <th>Loisir</th>
                <td>{{ hobby.loisir }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hobbies_index') }}">back to list</a>

    <a href="{{ path('app_hobbies_edit', {'id': hobby.id}) }}">edit</a>

    {{ include('hobbies/_delete_form.html.twig') }}
{% endblock %}
